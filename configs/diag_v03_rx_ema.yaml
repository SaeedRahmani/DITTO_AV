# v0.3.2 axis-5 ATTRIBUTION variant (V032_PLAN 1.3.5): the UNCHANGED v0.3
# clp_rx checkpoint with test-time plan smoothing only (tracker EMA
# 0.5 ~ 0.2 s lag). Bounds how much of the CARLA jiggle is
# tracker-side vs model-side. Not judged by S1/driving gates.
run_config: /home/srahmani/ditto_out/b2d_v03_rx_cpu.yaml
policy: clp_rx
stochastic: false
action_repeat: 2
stuck_recovery: true
tracker:
  ema: 0.5
