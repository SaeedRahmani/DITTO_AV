# v0.2 G3 baseline: the same TokenPolicy trained by sequence BC only —
# the controlled comparison arm (same net/data/tracker; only the
# closed-loop objective differs).
run_config: /home/srahmani/ditto_out/b2d_v02_cpu.yaml
policy: clp_bc
stochastic: false
action_repeat: 2
stuck_recovery: true
